# Quinn mockup parity

Reference: `/home/james/Downloads/quinn-product-implementation-spec-reconciled.html`, reconciled revision 4. This tracks functional differences as well as appearance. Reference sample names, counts and successful checks must be replaced with real authorized data.

The requested scope includes all 17 connected views and their dialogs. A checkbox here means implemented and verified, not merely present in the prototype.

| Surface                   | Present                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Remaining parity work                                                                                                                                                         |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Overview                  | Quinn navigation; independent use cards; configuration-aware status; configuration destinations; permission-gated inbox shortcut; authorized attention list; a durable unconfirmed-effect signal that stays silent when there is nothing owed                                                                                                                                                                                                                                                                                                                                                                             | Real source/guidance/connection summaries, tested-draft actions                                                                                                               |
| Knowledge                 | Per-type and document/page per-row use switches; uploads; URL intake; Help Center management link; unified Add source menu; authorized stored-text previews and fetch metadata; per-row index health that stays quiet when a source is healthy and names a failed update, a never-indexed source and a keyword-only generation; a staged refresh that builds the new generation alongside the current one; help-center article index health summarized beside the document and web-page rows, silent when every article is healthy                                                                                        | Passage counts and reference lists; operator visibility of the backfill itself                                                                                                |
| Guidance                  | Canonical authored entries with separately authorized role bindings written in one transaction; lossless conversion of writing guidelines, situational rules and procedures, proved byte-identical in the compiled prompt; one store answers a runtime read, chosen by a cutover flag; versioned saves with stale-write refusal and retained drafts; filters/search; split editor; selection-change confirmation                                                                                                                                                                                                          | References/examples fields, saved drafts and publication state, conversation-to-guidance improvement, authoring a new procedure                                               |
| Connections               | External connectors; side-by-side customer/teammate rules and per-rule default reset for built-ins and for every connector tool; versioned per-use external policies with stale-write refusal; reviewed discovery/schema contracts with an explicit review action                                                                                                                                                                                                                                                                                                                                                         | Unified selection/editor; customer subject binding for external reads; workspace-use editing on this page                                                                     |
| Deploy                    | Customer/team controls; channel links; honest setup status; an Email row that reads real availability and carries the autonomous-reply switch once an inbound route and customer replies are both set up; managed Workspace/Slack row; collapsible identity/voice; the release-management opt-in with the live release and whether a candidate is waiting, rendered only once it is on                                                                                                                                                                                                                                    | Per-channel pause controls beyond the one switch; Slack availability still lives with its integration                                                                         |
| Test Quinn                | Sandbox chat against an exact behaviour that grounds on no conversation and previews every write; draft/live comparison on the same question; required checks with real results bound to the candidate hash; publish review with the affected-use diff and a gate that blocks a stale, skipped, failed or inconclusive required check; release history with rollback                                                                                                                                                                                                                                                      | Per-check run history and cancellation; a saved scenario library on the page; model-graded review of the optional suite                                                       |
| Improve                   | Existing involvement/action/team metrics; authorized recent approval/handoff/low-rating queue over conversations AND flagged ticket runs; current active list; 7/30-day review filter; durable run records exist to read; run, action and approval operations from the run rows, with honest empty states and a dash for any rate with no denominator; a guidance draft written from a flagged conversation                                                                                                                                                                                                               | Completed-conversation outcomes; a saved-case authoring screen; a guidance draft from a flagged ticket, which needs a ticket source column                                    |
| Workflows                 | Existing builder and Quinn blocks; `let_assistant_answer` parks the wait and requests its durable turn in one transaction, keyed by workflow run, node and visit; completion is correlated to that visit, later turns in the engagement inherit it, and a sweep recovers a wait whose run died or handed off without saying so; `call_tool` and `approval` procedure steps, authorable in the builder, receipted by run, node and visit so a re-walked visit reads the first attempt, governed by the workspace's own tools dial, with only a confirmed execution taking the approved edge                                | Run inspection UI for a delegation; connector tools and step-to-step arguments in a procedure; a third edge separating a refusal from an unconfirmed effect                   |
| Channel lifecycle         | Channel-specific built-in controls, custom/off ownership and retained drafts; every lifecycle transition invalidates in-flight Quinn work in its own statement; the built-in clock stands down while Quinn owes an approved action's result, and an undecided proposal releases the run holding it                                                                                                                                                                                                                                                                                                                        | Inactivity has no view of a deferred workflow wait; a delegation's expiry is visible only in the run timeline                                                                 |
| Help Center article       | Per-use switches and exclusion filter; an article is indexed as passages on every save and removed from the index on delete, so a fact anywhere in it is retrievable and a private or unpublished article leaves retrieval at once                                                                                                                                                                                                                                                                                                                                                                                        | Index health and passage detail on the article screens; contextual exact-candidate tests                                                                                      |
| Inbox                     | Ask Quinn and existing approval cards; the inline card reads the review decision and the execution as two separate states, approved-and-queued is never drawn as completed, a late result after takeover lands as a private note, and an unconfirmed effect offers a verdict with no repeat; run inspection beside the conversation with the trigger, timings, token cost, release and snapshot, selected evidence, validator verdict and action outcomes; stop and re-run controls that enforce their own preconditions; correcting an answer and keeping it as a regression case                                        | The customer-outcome dimension is deliberately not asserted by the card; no deep link into a single run                                                                       |
| Needs approval            | Its own inbox view and column in the AI section: proposals this viewer may decide and executions nobody confirmed, each row scoped by the item it belongs to and linking to it; decision and execution job commit together; the worker re-resolves approver, parent, policy, contract and arguments before dispatch                                                                                                                                                                                                                                                                                                       | Bulk reconciliation and filters; a recovery sweep for an execution whose job row aged out; workspace-thread proposals have no inbox parent to list under                      |
| Feedback                  | Internal post audience denied to the attributed customer and every public reader, count, feed, export, retrieval and API/MCP path; capture idempotent on the receipt's own action key; capture emits no vote, subscription, activity, announcement or webhook; provenance and conversation backlink on the detail; reviewed permission-gated publication exposing only the fields a person read; Internal captures filter; linking a capture to an existing request instead of merging; a reviewed follow-up naming every recipient and every reason one is unreachable, one email per post and customer, never automatic | Choosing the board at publication; captures still counted in the People drawer and analytics; no follow-up prompt when a post ships, and no second update about the same post |
| Messenger                 | Validated final answers and current-source checks; durable publication fences; reconnect restores in-flight run state; a retried send returns the original message; a run waiting on approval parks without leaving a typing indicator, and the action's real result returns as a fenced continuation that never claims an unconfirmed effect completed; capture_feedback records an internal note the customer cannot see, acknowledged as recorded rather than promised, and never drops a post card into the thread                                                                                                    | A customer-facing link exists only once a teammate publishes the capture to a board, and nothing offers that link back into the thread yet                                    |
| Public source/Help Center | Revocation-aware customer source endpoint, now also the publication fence's own recheck: an answer citing a source revoked mid-generation is refused rather than published with a link that will not open                                                                                                                                                                                                                                                                                                                                                                                                                 | Rich authorized source display without exposing private originals; showing the customer which passage an answer came from                                                     |
| Email                     | Existing conversation and teammate email support; autonomous Quinn replies behind an explicit channel switch and five per-conversation rules (verified sender, a Message-ID to thread from, an address, nobody else owning it), delivered by a durable job that re-checks them, with a delivery record on the message and real threading; the Deploy row reads the real state                                                                                                                                                                                                                                             | Quinn never starts a thread and never answers a cold first message; no per-conversation opt-out; no separate Quinn sending identity                                           |
| Workspace/Slack           | Existing managed defaults and linked teammate authority; a workflow procedure step acts under the workspace role, so its result is never reported to the customer as a Quinn continuation                                                                                                                                                                                                                                                                                                                                                                                                                                 | Consistent source/action inspection on the linked surface; a workspace-use editor for connector policies, which is what a procedure step would need to call a connector tool  |

## Dependency order

Follow specification steps 3–12: durable run/snapshot/publication primitives; independent connection contracts; replayable actions and queued approvals; workflow/lifecycle waits; internal feedback privacy; canonical guidance; staged retrieval/evidence; exact-candidate evaluation/publication; operational inspection; channel extensions. Styling and navigation improvements can proceed earlier where the current backend supports their claims.

No Redis is required: durable work uses the existing PostgreSQL job queue and transactional outbox. No push, merge or deployment is included.
